module behavior;

object system
{
	name "system_0";
    tau 1;
    N 10;
    mu 1.0;
    u 0,1;
}

class device
{
	double x;
}

#for DEV in ${RANGE 0,10}

object device
{
	name "device_${DEV}";
}
modify system_0.device "device_${DEV}.x";
#done

#set savefile=test_device.json
