Anyone may submit a pull request to the `develop` branch for consideration in the next release. Only the `develop` branch may be merged to `master`. 

Pull requests from new contributors are reviewed by the TSC and merged by maintainers after approval. All other pull requests are reviewed and merged by maintainers as needed.

Releases are considered minor if existing capabilities are deprecated.

Releases are considered major if deprecated capabilities are removed.
